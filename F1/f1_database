<?php
$host = "localhost";
$user = "root"; // ถ้าใช้ XAMPP, ค่าเริ่มต้นคือ root
$pass = ""; // ค่าเริ่มต้นคือค่าว่าง
$dbname = "f1_database";

$conn = new mysqli($host, $user, $pass, $dbname);

if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}

?>
